{
  "name": "enterprise-saas-api",
  "version": "1.1.0",
  "description": "Production-Ready B2B API with PostgreSQL",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "pg": "8.11.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}